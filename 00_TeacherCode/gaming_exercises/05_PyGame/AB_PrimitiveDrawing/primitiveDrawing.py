# Primitive Drawing, Ryan Kelley, v0.4
import pygame, sys
from pygame.locals import *

pygame.init() 

# Setup Window 
DISPLAYSURF = pygame.display.set_mode((500, 400), 0, 32)
pygame.display.set_caption('Primitive Drawing')
